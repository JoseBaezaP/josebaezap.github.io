---
import GithubSlugger from 'github-slugger'
const slugger = new GithubSlugger()
const { title } = Astro.props;
const slug = slugger.slug(title);
---
<h3 class="text-2xl sm:text-3xl font-bold" id={slug}>{title}</h3>
